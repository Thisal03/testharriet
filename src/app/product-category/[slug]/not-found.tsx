import React from "react";
import NotFoundSection from "@/components/not-found-section";

const NotFound = () => {
  return (
    <NotFoundSection
      title="Category Not Found"
      subtitle="We can't find the category you are looking for"
    />
  );
};

export default NotFound;
