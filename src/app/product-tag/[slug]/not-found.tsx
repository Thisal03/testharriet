import React from "react";
import NotFoundSection from "@/components/not-found-section";

const NotFound = () => {
  return (
    <NotFoundSection
      title="Product Tag Not Found"
      subtitle="We can't find the page you are looking for"
    />
  );
};

export default NotFound;
