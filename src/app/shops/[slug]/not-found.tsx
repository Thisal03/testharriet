import React from "react";
import NotFoundSection from "@/components/not-found-section";

const NotFound = () => {
  return (
    <NotFoundSection
      title="Shop Not Found"
      subtitle="We can't find the shop you are looking for"
    />
  );
};

export default NotFound;
